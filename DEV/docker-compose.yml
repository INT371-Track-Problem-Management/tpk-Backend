version: "2.2"
services:
  backend:
    container_name: "backendDev"
    image: ghcr.io/artidsun/int371-backend-dev:latest
    ports:
      - "3000:3000"
    restart: on-failure
    env_file:
      - ../.env
networks:
  default:
    external:
      name: project-net
