version: "3.3"
services:
  backend:
    container_name: "backend"
    image: ghcr.io/artidsun/int371-backend:latest
    ports:
      - "5000:5000"
    restart: on-failure
networks:
  default:
    external:
      name: project-net
